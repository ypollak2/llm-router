# North Star audit — 2026-09-24

Audit of `main` @ `d6d5d1d` against [NORTH_STAR.md](NORTH_STAR.md). Six independent,
read-only reviewers each took one group of points. The most surprising findings were
re-run by hand (marked ✔). Ledger figures come from the maintainer's `~/.llm-router/` on
2026-09-24.

**Result: 7 met · 8 partial · 5 not met** (7, 8, 10, 13, 20).

| # | Point | Verdict | Evidence | Main gap |
|---|---|---|---|---|
| 1 | Preserve quota, not dollars | PARTIAL | `hooks/session-end.py:716-721` says "quota preserved" in subscription mode | That figure sums all routed tokens, used or not; `status` and `summary` speak only `$` |
| 2 | Flat-rate subscriber first | PARTIAL | Seats detected at install (`seats.py:88-103`) | Subscription mode needs `LLM_ROUTER_CLAUDE_SUBSCRIPTION=true` (`discover.py:174-197`) |
| 3 | Zero keys, no workflow change | MET | No keys, no Ollama → advisory, no crash (`discover.py:226-251`) | Large install footprint: `~/.claude/settings.json`, hooks, rules, and `~/.codex/*` by default (`commands/install.py:583-757`) |
| 4 | Route by complexity | PARTIAL | Complexity floor (`classify.py:352-364`); no Ollama for code/research | Capability registry `with_capability()` has no caller in the routing path |
| 5 | Local-first | PARTIAL | No analytics SDK; OAuth usage call reads the user's own quota | ✔ Session start fetches from huggingface.co, GitHub and LiteLLM with no opt-in (`hooks/session-start.py:1090-1139`, `:1398`) |
| 6 | Count only used answers | PARTIAL | Hook ledger gates on `realized=_turn_blocked` (`hooks/savings_logger.py:4455`) | ✔ `summary` reads `lineage.db` with no realized filter (`observability/summary.py:115-160`) |
| 7 | Never trade quality for quota | **NOT MET** | Quality measured well offline (`docs/BACKEND-QUALITY.md`) | No quality gate in `pre-release-verify.sh` or CI; RouterArena is a proxy population; 16,869 of 17,195 quality rows are test noise |
| 8 | status/summary: verified vs unverified, with n | **NOT MET** | Only `savings-report` uses the canonical accessor | `status`'s "unverified" means test traffic, not "unused"; `summary` has no split |
| 9 | Fail open to Claude, never a wrong answer | PARTIAL | Demotion wired (`chain_builder.py:60-95`, bandit `router.py:4232-4268`) | ✔ Only a liveness check: judge wired but **0 of 1,608** routing decisions graded (`usage.db`, `telemetry.py:81-98`) |
| 10 | Right-sized, no org creep | **NOT MET** | — | ✔ `control_plane/` (1,781 lines, tenants, Postgres store, signed policy) + `llm-router cp` (`cli.py:864`, `:1009`) |
| 11 | Every number carries n/window/source | PARTIAL | CHANGELOG 15.x and `docs/MEASUREMENT.md` comply | README keeps "35–80%"/"87%" as anecdotes; ✔ `docs/BENCHMARKS.md` says "auto-updated every Monday", last updated 2026-03-30 |
| 12 | Savings are quota; API figures labelled | PARTIAL | `label_money()` (`savings.py:394-405`) | Used only by `savings-report`; `status`/`summary` print bare `$` |
| 13 | Unverified never a headline | **NOT MET** | `status` puts verified first | ✔ `summary` headline: `Session savings $0.1125 (100% vs always-premium)` — all of it unverified |
| 14 | Measure real workload, prove the filter | MET | `scripts/groundtruth/sources.py` with named `DROP_*` reasons, used by `measure_low_signal_rate.py` | — |
| 15 | Claims match the mode | MET | README qualifies the headline (`README.md:19-25`); savings writes gated on `_turn_blocked` | "🎯 routed →" stderr banner prints on every draft, used or not |
| 16 | Draft is an unverified hint | MET | `hooks/response_formatter.py:118-121` "UNVERIFIED DRAFT … never as fact" | — |
| 17 | Safety claims scoped | MET | Blocklist (`hooks/agent_loop.py:130-141`) matches README and `SECURITY.md:327-344`, pinned by `tests/test_direct_execution_blocklist_coverage.py` | — |
| 18 | Every decision logged with a reason | MET | `tests/test_routing_outcome_logged.py` 9 passed; live log since 2026-09-14: 2,494 of 2,501 invocations have an outcome (99.7%) | 49.8% of real traffic is decided by the low-signal default (disclosed) |
| 19 | Publish negative results | MET | `docs/ROUTERARENA.md:83-97`, `README.md:156-160` | Today's failed delegation A/B (`7b393fe`) is only in `architecture/Q15_HARNESS_PLAN.md` |
| 20 | Ship only through the verified path | **NOT MET** | Versions agree everywhere (15.2.0); each tag has a Release | PyPI publish run 36030865530 failed (OIDC 403), 15.2.0 uploaded by hand; 45 of 47 commits on 2026-09-24 went straight to main, including the release commit `09ff0df` |

## Primary metric

**Verified share of eligible Claude turns replaced: ~0.66%** — 7 realized of 1,066 gated
`host=claude_code` rows in `usage.db.savings_stats` since the realized gate (all time to
2026-09-24). No CLI surface shows this number.

## Found outside the 20 points

- ✔ `llm-router summary` is branded **"⚡ C H U Z O M ⚡"** (`observability/summary.py:46`,
  `banner_art.txt:2`). Chuzom is a separate, retired product.

## Top fixes, by impact on the North Star

1. `summary`: add the verified/unverified split with n, label counterfactual dollars as such,
   and fix the brand (points 6, 8, 12, 13).
2. `status`: make "unverified" mean "not used", via `savings.py`'s realized predicate (point 8).
3. Get the judge producing rows on real traffic; until then, say "liveness", not "quality" (points 7, 9).
4. Add a quality step to `pre-release-verify.sh` (point 7).
5. Fix the PyPI trusted publisher; route release commits through a PR (point 20).
6. Decide on `control_plane/`: move it to Chuzom, or change point 10 (point 10).
7. Make the session-start benchmark fetch opt-in (point 5).
8. Either run the `docs/BENCHMARKS.md` refresh workflow or drop the "every Monday" line (point 11).
