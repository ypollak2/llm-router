# North Star re-audit — 2026-09-25

Re-audit of `main` @ `47ba17d`, after PRs #143–#148 merged, against [NORTH_STAR.md](NORTH_STAR.md).
Three independent read-only reviewers each checked one group of points. Prior verdicts come from
[AUDIT-2026-09-24.md](AUDIT-2026-09-24.md).

**Result: 13 met · 6 partial · 1 not met** (previously 7 · 8 · 5). Nothing regressed.

> **Caveat that governs every savings verdict below:** the fixes are in `main`, but none of them is
> running on the maintainer's machine. `UserPromptSubmit` runs the uv-tool install **v15.2.0**; the
> other hooks run the repo `.venv` from a checkout still at `d6d5d1d`; and the hook scripts in
> `~/.claude/hooks/` are copies made at install time. `savings_stats` has no row newer than
> 2026-09-24T20:31Z, and `judge_queue.jsonl` doesn't exist. These are **code verdicts, not live ones**.

| # | Point | 09-24 | 09-25 | Evidence | Remaining gap |
|---|---|---|---|---|---|
| 1 | Quota, not dollars | PARTIAL | **MET** | `hooks/session-end.py:772-789`: realized-only tokens, plus "(N unmeasured, not counted)" | Not yet seen on live data |
| 2 | Subscriber first | PARTIAL | PARTIAL | `install.py:236-247` detects the seat but doesn't set the mode | #151, open |
| 3 | Zero keys, no workflow change | MET | MET | Advisory fallback intact | Large install footprint |
| 4 | Route by complexity | PARTIAL | PARTIAL | `with_capability()` still has no caller | Parked |
| 5 | Local-first | PARTIAL | **MET** | Benchmark fetch gated (`session-start.py:41-50`, `1135-1189`); judge drain local-only (`judge.py:227-263`) | — |
| 6 | Count only used answers | PARTIAL | **MET** | Verified predicate requires `mode='block'`; NULL → `unmeasured_n` (`dashboard_data.py:320-326`) | Not yet seen on live data |
| 7 | Quality gate | NOT MET | PARTIAL | Classifier gate `accuracy 1.000 (n=60)`, blocking; quality gate fails loudly | 3/3 baseline in #150 (open); weak judge (0.667 for a wrong answer) not documented in the repo |
| 8 | Verified vs unverified, with n | NOT MET | **MET** | `status` and `summary` both split them, with the right n | — |
| 9 | Fail open, never wrong | PARTIAL | PARTIAL | Judge root causes fixed in `main` | Live 0 of 1,608 graded: the fix isn't installed |
| 10 | No org creep | NOT MET | NOT MET | `control_plane/` unchanged | Parked by owner |
| 11 | Numbers carry n | PARTIAL | PARTIAL | "35–80%/87%" removed | `README.md:290` "~70%" has no n; `BENCHMARKS.md` stale until the first cron run (2026-09-28) |
| 12 | $ labelled | PARTIAL | **MET** | `label_money` used in `status` and `summary` too | — |
| 13 | Unverified never headline | NOT MET | **MET** | Verified headline first; unverified below, with n | — |
| 14–17 | Measurement, mode claims, draft hint, safety scope | MET | MET | Preserved | — |
| 18 | Every decision logged | MET | **MET, stronger** | `routing_rate.py --since 2026-09-24`: 515 real prompts, 0 UNLOGGED | — |
| 19 | Negative results | MET | MET (gap closed) | Q15 in CHANGELOG and `BACKEND-QUALITY.md`; numbers match the source | — |
| 20 | Verified release path | NOT MET | PARTIAL | Every commit since `d6d5d1d` came through a PR merge | PyPI trusted publisher still 403s (D1) |

## Primary metric

`Verified share of eligible Claude turns (all-time): too few to tell (n=0) · unmeasured n=122 (mode not recorded)`

The line now exists and is honest. It can only move once the fixed writer is installed and new rows
carry `mode`. Re-measure after the install is updated and a few days of real use have passed.

## Also found

- The live hooks run on two interpreters at two code versions (uv tool 15.2.0 and the repo `.venv`).
- A test from PR #149 (`test_s_run_command_sequences.py::test_a_pipeline_runs`) failed once on Python 3.11 CI and passed on re-run. It's flaky.
