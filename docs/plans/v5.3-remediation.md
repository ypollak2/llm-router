# v5.3.0 Remediation Plan — Second Audit

Generated from senior production audit of v5.2.0.
Target release: v5.3.0 (single version).

---

## Phase A — Quick Wins (< 1 day total, low risk)

| # | Task | File(s) | Risk |
|---|------|---------|------|
| A1 | Dedup model chain after all injections | router.py | Low |
| A2 | Fix sync read in `_claude_subscription_state()` → `asyncio.to_thread` | budget.py | Low |
| A3 | Fix `open()` without context manager | hooks/auto-route.py | Low |
| A4 | Fix Ollama default model name (`gemma4` → `gemma3`) | hooks/auto-route.py | Low |
| A5 | Add `correlation_id` column to usage + routing_decisions tables | cost.py | Low |
| A6 | Emit `correlation_id` from `route_and_call()` into DB rows | router.py, cost.py | Low |

## Phase B — Data Integrity (1–2 days)

| # | Task | File(s) | Risk |
|---|------|---------|------|
| B1 | Add DB indices on `usage(provider, timestamp)` and `routing_decisions(timestamp)` | cost.py | Low |
| B2 | Fix TOCTOU in budget enforcement — provisional spend deduction before call | router.py, cost.py | Medium |
| B3 | Add `usage.json` staleness guard — treat pressure as 0.5 if no file for 24h | budget.py | Low |
| B4 | Document session spend reset limitation in docstring + README | session_spend.py, README.md | Low |

## Phase C — Refactor (2–3 days)

| # | Task | File(s) | Risk |
|---|------|---------|------|
| C1 | Extract `_resolve_profile()` from `route_and_call()` | router.py | Medium |
| C2 | Extract `_build_and_filter_chain()` from `route_and_call()` | router.py | Medium |
| C3 | Extract `_dispatch_model_loop()` from `route_and_call()` | router.py | High |
| C4 | Extract `_record_success()` from `route_and_call()` | router.py | Low |
| C5 | Move `configure_logging()` call out of `route_and_call()` → server startup | router.py, server.py | Low |

## What NOT to Fix (Low ROI — confirmed)

- macOS-only OAuth inline refresh (cross-platform Keychain = 3-week project)
- Prompt hash privacy (single-user localhost tool)
- Research fallback prompt rewriting (complex NLP, marginal impact)
- `provider_budget.py` naming (rename opportunistically)

---

## Release Checklist

After all phases complete:
1. `uv run pytest tests/ -q --ignore=tests/test_integration.py`
2. `uv run ruff check src/ tests/`
3. Bump version to 5.3.0 in pyproject.toml + plugin.json + marketplace.json
4. Add CHANGELOG.md entry
5. Commit + push + PyPI + git tag + GitHub release + plugin reinstall
