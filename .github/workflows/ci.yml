name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  UV_HTTP_TIMEOUT: 30

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
        continue-on-error: true
        id: uv-setup
      - name: Retry uv setup
        if: steps.uv-setup.outcome == 'failure'
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: false
      - run: uv python install 3.11
      - run: uvx ruff check src/ tests/

  identity-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
        continue-on-error: true
        id: uv-setup
      - name: Retry uv setup
        if: steps.uv-setup.outcome == 'failure'
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: false
      - run: uv python install 3.11
      - name: Check brand identity (no upstream-name leakage)
        run: python scripts/ci/check_identity.py

  version-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
        continue-on-error: true
        id: uv-setup
      - name: Retry uv setup
        if: steps.uv-setup.outcome == 'failure'
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: false
      - run: uv python install 3.11
      - name: Verify version sync
        run: uv run python scripts/ci/verify-version-sync.py

  plugin-sync-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
        continue-on-error: true
        id: uv-setup
      - name: Retry uv setup
        if: steps.uv-setup.outcome == 'failure'
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: false
      - run: uv python install 3.11
      - name: Verify plugin distribution sync
        run: python scripts/ci/verify-plugin-sync.py

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
        continue-on-error: true
        id: uv-setup
      - name: Retry uv setup
        if: steps.uv-setup.outcome == 'failure'
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: false

      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run tests
        shell: bash
        env:
          # A routing-audit fix means several test modules need SOME candidate
          # in the provider chain. They patch the dispatch layer and make no
          # network call, but a bare runner has no keys and no Ollama, so the
          # chain builds empty and they die with "No providers available".
          #
          # Kept identical to the value in scripts/release/pre-release-verify.sh:
          # two runners of one suite that disagree about its environment is how
          # a release gate ends up contradicting CI.
          OPENAI_API_KEY: sk-test-dummy-key-for-ci-only-no-real-calls-made
        run: |
          # Run pytest in the background so we can detect the aiosqlite thread hang.
          # pytest writes junit xml before the hang; we read that to determine pass/fail.
          uv run pytest tests/ -q \
            --ignore=tests/test_agno_integration.py \
            --timeout=30 \
            --junit-xml=/tmp/pytest-results.xml &
          PYTEST_PID=$!

          # Wait up to 900s for pytest to exit normally.
          #
          # Was 150s, sized for a ~2,700-test suite. The upstream-core sync took
          # it to ~6,900, so the wait expired at 31% progress, the hang-handler
          # fired on a pytest that was working perfectly, found no junit xml
          # (not written yet) and reported failure. Nothing was broken; the
          # watchdog was simply shorter than the work.
          #
          # 900s is the ceiling, not the expectation: a healthy run finishes in
          # ~4 minutes and exits the loop immediately. This only bounds the
          # aiosqlite exit-hang the handler below exists for. timeout-minutes
          # on the job is raised to match, or the job dies before the loop can.
          for i in $(seq 1 900); do
            if ! kill -0 $PYTEST_PID 2>/dev/null; then
              wait $PYTEST_PID
              exit $?
            fi
            sleep 1
          done

          # Still running after 900s — pytest is hanging on exit (aiosqlite thread).
          # Read the junit xml written before the hang to determine true pass/fail.
          echo "pytest is hanging on exit — reading junit results..."
          python3 - <<'PY'
          import sys, xml.etree.ElementTree as ET
          try:
              root = ET.parse("/tmp/pytest-results.xml").getroot()
              suite = root if root.tag == "testsuite" else root.find("testsuite")
              failures = int(suite.get("failures", 0))
              errors   = int(suite.get("errors",   0))
              tests    = int(suite.get("tests",     0))
              print(f"junit: {tests} tests, {failures} failures, {errors} errors")
              sys.exit(1 if (failures or errors) else 0)
          except Exception as e:
              print(f"Could not read junit xml: {e}")
              sys.exit(1)
          PY
          result=$?
          kill $PYTEST_PID 2>/dev/null || true
          exit $result
